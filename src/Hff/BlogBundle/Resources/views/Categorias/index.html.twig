{% extends '::base.html.twig' %}

{% block title %}Categorías{% endblock%}

{% block body -%}
    <h1>Lista de Categorías</h1>
    <select>
        <option value="0">Selecciona la Categoría</option>
    {% for entity in entities %}
            <option value="{{entity.nombre}}">{{entity.nombre}}</option>
    {% endfor %}
    </select>
    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Imagen</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('categoria_ver', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.imagen }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('categoria_ver', { 'id': entity.id }) }}">Mostrar</a>
                    </li>
                    <li>
                        <a href="{{ path('categoria_editar', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('categoria_nueva') }}">
                Ingresar una nueva categoría
            </a>
        </li>
    </ul>
    {% endblock %}
