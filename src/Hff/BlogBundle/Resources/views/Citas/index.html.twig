{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Citas list</h1>

    <table class="records_list" border="1">
        <thead>
            <tr>
                <th>Id</th>
                <th>Texto</th>
                <th>Autor</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('citas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.texto }}</td>
                <td>{{ entity.autor }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('citas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('citas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('citas_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
