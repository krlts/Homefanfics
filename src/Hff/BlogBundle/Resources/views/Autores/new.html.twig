{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ingresar un Autor</h1>

    <form action="{{ path('autores_new') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Crear</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('autores') }}">
            Volver a la Lista
        </a>
    </li>
</ul>
{% endblock %}
